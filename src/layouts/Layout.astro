---
import "../styles/global.css";

import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";
import WebMeta from "../lib/WebMeta.astro";
import Navbar from "../components/common/Navbar.astro";
import Footer from "../components/common/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  showFooter?: boolean;
  stickyNavbar?: boolean;
}

const { title, description } = Astro.props;
const { showFooter = true, stickyNavbar = false } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <WebMeta title={title} description={description} />
    <link rel="icon" type="image/svg+xml" href="/assets/jfavico.png" />

    <link
      rel="preload"
      href="/fonts/subset/PlusJakartaSans-Bold-Subset.otf"
      as="font"
      type="font/otf"
      crossorigin
    />
  </head>
  <body class="bg-white">
    <script src="https://unpkg.com/react-scan/dist/auto.global.js"></script>
    <Navbar stickyNavbar={stickyNavbar} />
    <slot />
    {showFooter && <Footer />}
    <SpeedInsights />
    <Analytics />
  </body>
</html>

<style>
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html,
  body {
    height: 100%;
    width: 100%;
    font-family: "Plus Jakarta Sans Variable", sans-serif;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    margin: 0;
    padding: 0;
  }
</style>
